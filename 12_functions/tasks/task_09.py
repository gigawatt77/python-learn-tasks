"""
ЗАДАЧА 9 (посложнее):
Напиши функцию average(numbers), которая принимает список чисел и
возвращает их среднее арифметическое (сумма всех чисел, делённая на
их количество). Используй цикл for внутри функции, чтобы посчитать
сумму (не используй sum()). Вызови функцию со списком grades и
напечатай результат.

grades = [5, 4, 5, 3, 4, 5]

Пример вывода:
4.333333333333333
"""


def average(numbers):
    total = 0
    # TODO: перебери numbers циклом for и прибавляй каждое число к total,
    # затем верни total, делённое на len(numbers)
    return 0


grades = [5, 4, 5, 3, 4, 5]

# TODO: вызови average(grades) и напечатай результат
print("TODO: реши задачу выше")
